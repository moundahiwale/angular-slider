import { Routes } from '@angular/router';
import { UserSliderComponent } from './components/user-slider/user-slider.component';

export const routes: Routes = [{path: '', component: UserSliderComponent}];
